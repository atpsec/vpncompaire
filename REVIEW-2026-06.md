# İNCELEME & DEVİR RAPORU — vpncompaire

**Tarih:** 2026-06-01
**İnceleyen:** Claude Opus 4.8 (proje Opus 4.7 ile yapılmıştı)
**Amaç:** Yeni bir Claude hesabının projeye kaldığı yerden, bu raporu yol haritası olarak kullanarak devam etmesi.

> 🟢 ÖNCE OKU: Bu dosyadan önce sırasıyla `HANDOFF.md`, `CLAUDE.md`, `AGENTS.md` ve
> `C:\Users\ahmet\.claude\CLAUDE.md` (kullanıcı tercihleri) okunmalı. Bu rapor onların üstüne biner;
> proje yapısını/komutlarını tekrar etmez, **sadece yapılacak işleri** listeler.

---

## 0) Çalışma kuralları (kullanıcı = Ahmet)

- **Türkçe konuş.** Açıklamalar kısa ve doğrudan.
- **Onaysız yapma:** `git push`, prod deploy, force push, `--no-verify`, dependency ekleme/silme, 3. taraf servise veri gönderme. Her biri için önce tek cümleyle ne yapacağını söyle, sor.
- **Her değişiklikten sonra:** `npm run lint` (error 0) + `npm run build` (exit 0). Sonucu yanıtında belirt.
- **`git add .` yok** — dosya bazlı stage. WIP dosyalarına dokunma (aşağıda).
- Windows: deploy'da `NODE_OPTIONS="--use-system-ca"`, `getaddrinfo()` hatasında 3-5 sn bekle retry.

### Dokunulmayacak WIP dosyalar (git "modified" gösterir, görmezden gel)
- `src/app/[locale]/guvenlik-araclari/page.tsx`
- `src/app/[locale]/guvenlik-araclari/_body.tsx`
- `src/app/[locale]/rehber/page.tsx`
- `src/app/[locale]/sunucu-haritasi/page.tsx`
- `.claude/`

---

## 1) Genel değerlendirme

Proje olgun. Mimari temiz, tipleme bilinçli, güvenlik başlıkları çoğu ticari siteden iyi, i18n disiplinli (**tr/en 809 key, sıfır fark — MISSING_MESSAGE riski yok**), legal/disclosure katmanı ciddi. Opus 4.7 sağlam iş çıkarmış. **Puan ~7.5/10.**

Ama "bitmiş" değil. Üç sistemik zayıflık:
1. **i18n sızıntıları** — kod doğru, ama içerik/metadata katmanında EN locale'de Türkçe metin ve yanlış canonical'lar var (doğrudan SEO kaybı).
2. **Serverless'a uymayan kalıplar** — in-memory rate limit + cache yok; Vercel'de pratikte çalışmıyor.
3. **İçerik enflasyonu** — 100 yazının önemli kısmı birbirini yiyen (cannibalization) ince içerik; bazı VPN verileri blog ile çelişiyor.

### Çapraz-doğrulamada ayıklanan YANLIŞ POZİTİFLER (bunları düzeltme, sorun değil)
- **`/metodoloji` sayfası MEVCUT** (`src/app/[locale]/metodoloji/page.tsx`). "Kırık link" değil.
- **`inceleme/[slug]` generateStaticParams'ın sadece `{slug}` döndürmesi DOĞRU.** `[locale]/layout.tsx` locale'i üretiyor; Next.js nested segment'lerde kartezyen çarpım yapıyor. Yine de güvence için `npm run build` çıktısında `/en/inceleme/*` rotalarının üretildiğini bir kez gör.
- Tüm URL slug'ları EN'de de Türkçe (`sozluk`, `rehber`, `cihazlar`) — **kasıtlı tasarım**, sorun değil.
- `quiz.questions.*.helper` her iki dilde boş string — **kasıtlı** (koşullu render).

---

## 2) AKSİYON PLANI — öncelik sırasıyla

> Her madde: `dosya:satır` — bulgu — yapılacak. Severity: 🔴kritik / 🟠orta / 🟡düşük.

### ADIM 1 — Hızlı + yüksek etki (≈ yarım gün, düşük riskli, hepsi i18n/veri)

**1.1 🔴 EN canonical TR URL gösteriyor (25 sayfa, SEO).**
`src/lib/site.ts` → `absoluteUrl()` locale almıyor. `/en/sozluk` canonical'ı `.../sozluk` (TR) veriyor → Google EN'i duplicate sayabilir.
- `absoluteUrl(path, locale?)` imzasına geç: default locale değilse `/${locale}` prefix ekle.
- Tüm sayfa `generateMetadata`'larında `alternates.canonical`'a locale geçir.
- Aynı anda sayfa bazlı `alternates.languages` (hreflang tr/en) ekle — şu an sadece root layout'ta global var, sayfa bazında yok. Etkilenen: tüm `/araclar/`, `/cihazlar/`, `/sozluk`, `/rehber/[slug]`, `/inceleme/[slug]`, `/karsilastir/*`, `/sana-uygun-vpn`, `/hesaplayici`.

**1.2 🔴 EN locale'de Türkçe metin sızıntıları.**
- `src/app/[locale]/cihazlar/{android,iphone,ipad,smart-tv}/page.tsx` — `export const metadata` statik + Türkçe hardcoded. → `generateMetadata({params})`'a çevir, `getTranslations` kullan. (Aynı sorun `karsilastir/*`, bazı `rehber/*`, `en-iyi/*` statik metadata'larında da var.)
- `src/data/devices.en.ts:8-11` — `DIFFICULTY = {easy:"Kolay", medium:"Orta", advanced:"İleri düzey"}` → İngilizce yap (`"Easy"/"Medium"/"Advanced"`) VE `src/components/device/device-page.tsx:26` CSS class map key'lerini de güncelle (key olarak bu string'leri kullanıyor).
- `src/components/home/hero.tsx:68` — `"312 saat test"` hardcoded → `t()`.
- `src/components/layout/site-header.tsx:34,103,109` — logo `aria-label="vpncompaire ana sayfa"`, mobil menü `"Dil / Language"`, `"Bizi takip et"` Türkçe hardcoded → `t()`.
- `src/components/brand/vpn-logo.tsx:39,45,51,84,189` — `aria-label="... logosu"` Türkçe → `{brand} logo` evrensel format veya `t()`.
- JSON-LD breadcrumb `"Ana sayfa"` hardcoded: `karsilastir/*`, `en-iyi/turkiye`, `rehber/vpn-nedir` vb. (blog'da `locale==="tr"?"Ana Sayfa":"Home"` ile doğru yapılmış — onu örnek al).

**1.3 🟠 Veri çelişkileri (güven + E-E-A-T). `products.ts` genelde DOĞRU, blog/glossary/features ESKİ.**
- `src/data/features.ts:115` TunnelBear `unlimitedDevices:false` ↔ `products.ts:793` "sınırsız". Karar ver, tek kaynağa hizala.
- `src/data/features.ts:33` ExpressVPN `openSource:true` yanıltıcı — sadece **Lightway protokolü** açık kaynak, istemci değil. Proton/Mullvad (tam açık) ile aynı flag yanlış. Ayrı bir "protokol açık kaynak" ayrımı düşün.
- NordVPN cihaz sayısı: `products.ts` "10" (doğru), `vpn-secerken...mdx:421` ve `nordvpn-vs-surfshark...mdx:70` "6" → blogları düzelt.
- Surfshark yargı: `products.ts` "Hollanda" (doğru), `vpn-secerken...mdx:98,440` "Panama/BVI" → düzelt.
- Mullvad port forwarding: `features.ts` `false` (doğru, Mullvad 2023'te kaldırdı), `glossary.ts:393` ve ilgili blog "destekliyor" → düzelt.
- CyberGhost `features.ts` `audited:true` ama `products.ts`'te denetim ismi yok — ya denetçi ekle ya flag'i gözden geçir.

**1.4 🟠 Kaynaksız istatistikler (güven + FTC riski).**
`src/content/blog/.../ucretsiz-vs-ucretli-vpn.mdx:59,87` — "%38 malware", "%84 IPv6 leak" atıfsız. Kaynak ekle veya kaldır. Ayrıca ProtonVPN Free "sadece 3 ülke" (satır 169) güncel değil.

### ADIM 2 — Orta (1-2 gün)

**2.1 🟠 blog.ts cache yok.** `src/lib/blog.ts` her istekte `fs`+`gray-matter`+`compileMDX`. `generateMetadata`+`Page` aynı postu 2× okuyor. `src/lib/blog-stats.ts:23,53,54` tek çağrıda `getBlogPosts`'u 3× çağırıyor. → `unstable_cache` / `"use cache"` ile sar; blog-stats'ı tek geçişe indir.

**2.2 🟠 Görsel optimizasyonu kapalı.** `unoptimized` kaldır: `src/components/blog/blog-card.tsx:28`, `src/components/blog/unsplash-image.tsx:39`, `src/components/home/IpSecurityBanner.tsx:124` (flagcdn). remotePatterns zaten tanımlı → AVIF/WebP devreye girer.

**2.3 🟠 Sitemap/RSS.** `src/app/sitemap.xml/route.ts:5` `revalidate` yok (yeni post yansımıyor) → `export const revalidate = 3600`. Sitemap'te eksik URL'ler: `/araclar` ve `/araclar/{ip-adresim,dns-leak-test,webrtc-leak-test,vpn-hiz-testi}`, `/cerez-politikasi`. `rss.xml/route.ts:5-6` `force-static`+`revalidate` çelişkili → `force-static`'i kaldır.

**2.4 🟠 ViewCounter Suspense'siz.** `src/app/[locale]/blog/[slug]/page.tsx:122` → `<Suspense fallback={null}>` ile sar (API key varsa sayfayı blokluyor).

**2.5 🟠 SpeedTester hata yutuyor.** `src/components/tools/SpeedTester.tsx:70-71` — ağ hatasında `setMbps(0)`, kullanıcı "0 Mbps" görüyor (hata mı gerçek mi belirsiz), ping `null` kalıyor. → ayrı `error` state + kullanıcı mesajı.

**2.6 🟠 DnsLeakTester leak kararı hatalı.** `src/components/tools/DnsLeakTester.tsx:47-53` — `example.com` A kaydını "farklı sunucu = sızıntı" sayıyor; yanıltıcı. Gerçek leak testi özel test sunucusu ister. Mantığı düzelt veya "deneysel" ibaresi koy. (WebRTC ve Speed test GERÇEK ve doğru çalışıyor — onlara dokunma.)

**2.7 🟠 Blog içi inline affiliate disclosure yok (FTC).** Blog yazıları VPN öneriyor + `/go` linki veriyor ama linkin yanında uyarı yok. Ayrı `/reklam-aciklamasi` sayfası mükemmel ama yeterli değil. → blog/MDX layout'una linkin yakınında kısa disclosure satırı ekle.

**2.8 🟡 a11y eksikleri.** quiz/calculator butonlarında `aria-pressed` yok; araç sonuçlarında `aria-live="polite"` yok (NewsletterForm'da doğru örneği var); quiz'de yeni soruya focus geçmiyor; skip-link hedefi `<main>`'e `tabindex="-1"`; tanımsız token'lar: `text-muted-foreground` (`view-counter.tsx:19`, `blog-stats.tsx:37`), `success-300` (`cost-calculator.tsx:90`) — `globals.css` ile hizala.

### ADIM 3 — Yapısal / güvenlik (proje işi, onay gerektirir)

**3.1 🔴 Rate limiting Vercel'de etkisiz.** `src/proxy.ts:7` + `src/app/api/newsletter/route.ts:9` in-memory `Map` — serverless'ta her instance izole, cold start sıfırlıyor. Ayrıca `proxy.ts:97` matcher `/go`'yu dışlıyor → affiliate redirect'leri middleware'e hiç uğramıyor. → Vercel KV/Upstash (sliding window) veya Vercel WAF. `/go/[slug]/route.ts`'e kendi limiti.

**3.2 🟠 CSP `script-src 'unsafe-inline'`.** `next.config.ts:18` — XSS sonrası script çalışmasını engelleyen katman devre dışı (`unsafe-eval` yok, o iyi). → nonce tabanlı CSP (middleware'de nonce üret, `headers()` ile geçir). `connect-src` `*.cloudflare.com` (satır 22) daralt; `source.unsplash.com` (satır 20, ölü domain) çıkar.

**3.3 🟠 server-only guard yok.** `src/env.ts` ve `src/lib/affiliate.ts` başına `import "server-only"` ekle — yanlış client import'unda affiliate URL'leri build-time hata verir, sızmaz.

**3.4 🟠 Newsletter PII log.** `src/app/api/newsletter/route.ts:113` tam email log'luyor (GDPR). `domain` değişkeni zaten hesaplı → sadece onu logla. Origin yoksa CSRF kontrolü bypass oluyor (satır 47-58) → sıkılaştır. Endpoint hiçbir yere kaydetmiyor ama "Abone oldun" diyor → ya gerçek servise bağla (Resend/Buttondown) ya beklentiyi düşür.

**3.5 🟡 plausible.ts.** `slug` encode edilmeden URL'e giriyor (`encodeURIComponent`); `env.ts` yerine `process.env` okuyor; `siteId` trailing slash'a kırılgan (`.replace(/\/$/,"")`).

**3.6 🟡 seo.ts JSON-LD eksikleri.** `websiteSchema` `inLanguage:"tr-TR"` hardcoded (locale almıyor); `articleSchema` `image` ImageObject sarıcısı + `wordCount` yok; `organizationSchema` `logo` yok; `itemListSchema` locale-aware değil. Review sayfasındaki JSON-LD inline — `seo.ts`'e taşı.

### ADIM 4 — İçerik konsolidasyonu (SEO, büyük iş)

**4.1 🔴 Cannibalization.** İki küme birbirini yiyor:
- "VPN neden/nasıl alınır": `vpn-nedir`, `vpn-gizlilik-ve-guvenlik`, `vpn-secerken...`, `ucretsiz-vs-ucretli`, `abonelik-donemi` — 5 yazı aynı intent.
- Streaming: pillar `vpn-streaming-ve-icerik-erisimi` alt yazıları (netflix/disney/spor/anime) tekrar anlatıyor.
→ Pillar+cluster'a indir: pillar = özet+link, alt yazılar derin. Birleştirilenlere 301/canonical. Hedef: 100 → ~70 güçlü yazı.

**4.2 🟠 EN blog kategori anahtarları Türkçe** (`temel-bilgiler` vb.) — `/en/blog` filtresinde Türkçe kategori görünebilir. Kontrol et.

**4.3 🟡 Bulk publish sinyali.** 50 TR yazı 2026-05-25..30 arası tek seferde yayında. İleride kademeli yayın daha sağlıklı.

---

## 3) EKLENMESİ ÖNERİLEN ÖZELLİKLER (etki sırasıyla)

1. **Türkiye'ye özel canlı performans/hız tablosu** — sitenin en büyük diferansiyasyon fırsatı; tüm hız iddiaları şu an doğrulanamaz. (HANDOFF'ta da "kritik".)
2. **VPN sahiplik & şeffaflık tablosu** (sahip/yargı/denetim geçmişi) — özellikle **ExpressVPN/Kape Technologies** sahipliği sitede hiç anılmıyor; piyasanın en büyük güven sorusu, E-E-A-T'yi en çok yükseltir.
3. **Türkiye + 5651 sayılı Kanun / erişim engeli güncel uyumluluk tablosu** — yerel arama niyetine birebir.
4. **Kullanıcı yorumları** (Vercel KV / JSON) + **fiyat takipçisi** (haftalık snapshot, grafik) — HANDOFF orta-yüksek önerileri.
5. **Yazar/E-E-A-T sinyali** — "Editör Ekibi" yerine tutarlı gerçek isim + kısa biyografi.
6. **Güvenlik olgunluğu paketi** — server-only + nonce CSP + KV rate limit (Adım 3).

---

## 4) ÇIKARILMASI / SADELEŞTİRİLMESİ GEREKENLER

- Cannibalize içerik (Adım 4.1).
- Newsletter: ya gerçek servis ya beklenti düşür (kayıt yapmıyor).
- Dead code: `src/lib/text-normalize.ts` uppercase replace'leri (toLocaleLowerCase sonrası asla eşleşmez), `src/lib/unsplash.ts` `christinHumeAlt` duplikat fotoğrafçı, `src/components/blog/social-share.tsx:277` kullanılmayan `locale` sr-only span.
- `unsplash.ts`: tek photo ID onlarca farklı `alt` ile reuse ediliyor (SEO için yanlış alt metin) — kademeli düzelt.
- CSP `source.unsplash.com` ölü allowlist girdisi.

---

## 5) Tam bulgu sayıları (referans)

6 paralel salt-okunur inceleme yapıldı: **güvenlik (12 bulgu), kod kalitesi/mimari (15), Next.js16/performans (14), i18n (6), UX/a11y (18), içerik/veri/SEO (12+).** Yukarıdaki plan bunların aksiyon alınabilir ve doğrulanmış olanlarını önceliklendirir. Severity'ler korundu; yanlış pozitifler (Bölüm 1) elendi.

**Önerilen başlangıç:** ADIM 1 tek oturumda toplu yapılabilir — hepsi düşük riskli, `lint`+`build` ile doğrulanır, sonra kullanıcıdan push/deploy onayı alınır.
